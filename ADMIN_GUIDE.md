запуск
1 терминал
cd backend
python -m venv venv
./venv/Scripts/activate
python.exe -m pip install --upgrade pip
pip install -r requirements.txt
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
2 терминал
python start_server.py

суперпользователь:

ALinAkunish@mail.com
qwerty1234

### Как войти

1. Откройте браузер и перейдите на страницу входа:
   ```
   http://localhost:8080/frontend/templates/login.html
   ```
   (или соответствующий адрес вашего сервера)

2. Введите **email администратора** в поле "Имя пользователя" (username)

3. Введите **пароль администратора** в поле "Пароль" (password)

4. Нажмите кнопку **"Войти"** или клавишу **Enter**

5. После успешного входа вы будете перенаправлены на главную страницу

### Проверка прав администратора

После входа вы можете проверить, что у вас есть права администратора:
- В личном кабинете (dashboard) должно быть указано, что вы администратор
- Вы сможете открывать админские страницы без ошибок

---

## 📋 УПРАВЛЕНИЕ ЗАКАЗАМИ

### Доступ к странице заказов

1. После входа в систему перейдите по адресу:
   ```
   http://localhost:8080/frontend/templates/admin-orders.html
   ```
   
   Или используйте навигационное меню на админских страницах:
   - Нажмите на ссылку **"Заказы"** в верхнем меню

2. Страница автоматически проверит ваши права администратора
   - Если у вас нет прав администратора, появится сообщение об ошибке

### Просмотр заказов

На странице заказов отображается таблица со всеми заказами от всех пользователей:
